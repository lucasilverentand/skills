# Tab views
A tab view displays several distinct content areas within a single frame, allowing users to switch between them using a dedicated tab control

## Platform guidance — watchOS
Tab views are rendered in watchOS using **page controls**. Developers seeking implementation guidance should consult [TabView](apple:SwiftUI/TabView) and [verticalPage](apple:SwiftUI/TabViewStyle/verticalPage).
