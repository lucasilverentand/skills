# Scroll views
A scroll view enables users to examine content that exceeds the display boundaries by shifting the content along a vertical or horizontal axis

## Platform guidance — tvOS
Views on tvOS support scrolling, but they are not treated as distinct objects requiring dedicated scroll indicators. Instead, when the content exceeds the screen dimensions, the system automatically scrolls the interface to ensure focused items remain visible.
