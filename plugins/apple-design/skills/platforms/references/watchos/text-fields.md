# Text fields
A text field is a rectangular region where users input or modify brief, specific textual data

## Platform guidance — watchOS
Display a text field only when it is essential. Whenever feasible, favor presenting a list of options rather than requiring the user to input text.
