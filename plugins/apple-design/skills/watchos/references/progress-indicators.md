# Progress indicators
Progress indicators inform users that your application is actively working, rather than being frozen, while it loads content or executes lengthy processes

## Platform guidance — watchOS
The system displays progress indicators in white by default, overlaid on the scene's background. To customize the indicator color, set its tint property.
