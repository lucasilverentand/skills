# Gradients
Starting with SF Symbols 7 and later, gradient rendering produces a smooth linear transition originating from a single source color. Gradients can be utilized across all rendering modes, supporting both system and custom colors as well as custom symbols. Although gradients display for symbols of any dimension, they achieve their best visual appearance when rendered at larger sizes.
